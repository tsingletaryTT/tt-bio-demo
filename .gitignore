__pycache__/
*.pyc
.venv/
env/
.venvs/
debian/.debhelper/
debian/debhelper-build-stamp
debian/files
debian/*.substvars
debian/*.log
debian/*.debhelper
debian/*.debhelper.log
debian/tt-bio-demo*/
*.deb
*.buildinfo
*.changes
.superpowers/

# tt-metal writes its Inspector/Watcher output to ./generated/ relative to the
# process CWD unless TT_METAL_LOGS_PATH is pinned. The daemon and the
# integration conftest both pin it (runner/env.py:runner_environ), but an
# ad-hoc script that constructs a Folder directly does not -- and that lands
# tens of MB per fold in the working tree. Ignored so it can never be
# committed by accident.
generated/

# Recorded booth demo video. Large binaries stay out of git -- masters live in
# recordings/ (also ignored) for recutting. The ONE exception is the short
# web-sized loop the README embeds, which has to be in the repo to render.
*.mp4
!docs/screenshots/*.mp4

# Recording masters: large local binaries, kept for recutting.
recordings/

# Ad-hoc screenshots dropped at the repo root by Spectacle during development.
# The README's images are the ones under docs/screenshots/, refreshed by
# scripts/refresh-screenshots.sh -- nothing here is referenced by anything.
/tt-bio-demo*.png

# Subagent worktrees (superpowers:subagent-driven-development).
.claude/worktrees/
